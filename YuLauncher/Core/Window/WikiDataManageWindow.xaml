<ui:FluentWindow x:Class="YuLauncher.Core.Window.WikiDataManageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:YuLauncher.Core.Window"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="ja-JP"
        lex:ResxLocalizationProvider.DefaultAssembly="YuLauncher"
        lex:ResxLocalizationProvider.DefaultDictionary="Language"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="WikiDataManageWindow" Height="400" Width="200"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                     Height="48"/>

        <Grid Grid.Row="1">
            <StackPanel Orientation="Vertical">
                    <Button x:Name="AddButton" Content="{lex:Loc AddGame}" Click="AddButton_OnClick"/>
                <ScrollViewer>
                    <WrapPanel x:Name="WrapPanel" Orientation="Horizontal">
                
                    </WrapPanel>
                </ScrollViewer>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="SaveButton" Content="{lex:Loc SimpleSave}" Click="SaveButton_OnClick"/>
                <Button x:Name="ExitButton" Content="{lex:Loc SimpleExit}" Click="ExitButton_OnClick"/>
            </StackPanel>
        </Grid>
        
    </Grid>
</ui:FluentWindow>
