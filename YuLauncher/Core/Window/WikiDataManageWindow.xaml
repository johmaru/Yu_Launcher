<ui:FluentWindow x:Class="YuLauncher.Core.Window.WikiDataManageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:YuLauncher.Core.Window"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="ja-JP"
        lex:ResxLocalizationProvider.DefaultAssembly="YuLauncher"
        lex:ResxLocalizationProvider.DefaultDictionary="Language"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="{lex:Loc WikiDataManageWindowTitle}"
        Width="600" Height="480"
        MinWidth="480" MinHeight="360"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{lex:Loc WikiDataManageWindowTitle}"
                     Height="48"/>

        <Grid Grid.Row="1" Margin="16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 追加ボタン -->
            <ui:Button Grid.Row="0"
                       x:Name="AddButton"
                       Content="{lex:Loc AddGame}"
                       Click="AddButton_OnClick"
                       HorizontalAlignment="Left"
                       Margin="0,0,0,12"/>

            <!-- ヘッダ -->
            <Grid Grid.Row="1" Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:TextBlock Grid.Column="0"
                              Text="{lex:Loc SimpleName}"
                              Appearance="Secondary"/>
                <ui:TextBlock Grid.Column="1"
                              Text="{lex:Loc SimpleAddress}"
                              Appearance="Secondary"
                              Margin="8,0,0,0"/>
                <ui:TextBlock Grid.Column="2"
                              Text="{lex:Loc DeleteGame}"
                              Appearance="Secondary"
                              Margin="12,0,0,0"/>
            </Grid>

            <!-- Wikiデータ一覧 -->
            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding WikiEntries, RelativeSource={RelativeSource AncestorType=local:WikiDataManageWindow}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ui:TextBox Grid.Column="0"
                                            Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <ui:TextBox Grid.Column="1"
                                            Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Margin="8,0,0,0"/>
                                <ui:Button Grid.Column="2"
                                           Content="{lex:Loc DeleteGame}"
                                           Click="DeleteEntry_OnClick"
                                           Tag="{Binding}"
                                           Margin="12,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- 保存ボタン -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="16,0,16,12">
            <ui:Button x:Name="SaveButton"
                       Content="{lex:Loc SimpleSave}"
                       Click="SaveButton_OnClick"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
