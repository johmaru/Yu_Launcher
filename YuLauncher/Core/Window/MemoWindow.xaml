<ui:FluentWindow x:Class="YuLauncher.Core.Window.MemoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:YuLauncher.Core.Window"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="ja-JP"
        lex:ResxLocalizationProvider.DefaultAssembly="YuLauncher"
        lex:ResxLocalizationProvider.DefaultDictionary="Language"
        mc:Ignorable="d"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Title="{lex:Loc MemoCtxHeader}"
        MinWidth="600" MinHeight="200"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid x:Name="Grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,0,0,1">
            <ui:TitleBar Title="{lex:Loc MemoCtxHeader}"
                         Height="48">
                <ui:TitleBar.Header>
                    <ToggleButton x:Name="ModeButton"
                                  IsChecked="False"
                                  Content="{lex:Loc EditModeButton}"
                                  Checked="ModeButton_OnChecked"
                                  Unchecked="ModeButton_OnUnchecked"/>
                </ui:TitleBar.Header>
            </ui:TitleBar>
        </Border>

        <Grid Grid.Row="1" Margin="12,4,12,12">
            <!-- 閲覧モード -->
            <ScrollViewer x:Name="ViewScrollViewer"
                          Visibility="Visible"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <ui:TextBlock x:Name="MemoTextBlock"
                              TextWrapping="Wrap"
                              Appearance="Primary"/>
            </ScrollViewer>

            <!-- 編集モード -->
            <Grid x:Name="EditPanel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ui:TextBox x:Name="MemoTextBox"
                            Grid.Row="0"
                            AcceptsReturn="True"
                            TextWrapping="Wrap"
                            VerticalScrollBarVisibility="Auto"
                            HorizontalScrollBarVisibility="Auto"
                            Margin="0,0,0,8"/>
                <ui:Button x:Name="SaveButton"
                           Grid.Row="1"
                           HorizontalAlignment="Right"
                           Content="{lex:Loc SimpleSave}"
                           Click="SaveButton_OnClick"/>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
