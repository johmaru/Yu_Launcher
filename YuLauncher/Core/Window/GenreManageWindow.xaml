<ui:FluentWindow x:Class="YuLauncher.Core.Window.GenreManageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:YuLauncher.Core.Window"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="ja-JP"
        lex:ResxLocalizationProvider.DefaultAssembly="YuLauncher"
        lex:ResxLocalizationProvider.DefaultDictionary="Language"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="{lex:Loc GenreManageWindowTitle}"
        Width="480" Height="400"
        MinWidth="360" MinHeight="320"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,0,0,1">
            <ui:TitleBar Title="{lex:Loc GenreManageWindowTitle}"
                         Height="48"/>
        </Border>

        <Grid Grid.Row="1" Margin="16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 追加入力 -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:TextBox Grid.Column="0"
                            x:Name="GenreText"
                            PlaceholderText="{lex:Loc GenreNameInput}"/>
                <ui:Button Grid.Column="1"
                           x:Name="AddButton"
                           Content="{lex:Loc AddGame}"
                           Click="AddButton_OnClick"
                           Margin="8,0,0,0"/>
            </Grid>

            <!-- ジャンル一覧ヘッダ -->
            <ui:TextBlock Grid.Row="1"
                          Text="{lex:Loc SimpleGenreManage}"
                          Margin="0,0,0,4"
                          Appearance="Primary"/>

            <!-- ジャンル一覧 -->
            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Genres, RelativeSource={RelativeSource AncestorType=local:GenreManageWindow}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Margin="0,2,0,2"
                                      IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                      Content="{Binding Name}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</ui:FluentWindow>
