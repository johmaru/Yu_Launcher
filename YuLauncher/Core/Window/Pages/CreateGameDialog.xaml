<ui:FluentWindow x:Class="YuLauncher.Core.Window.Pages.CreateGameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:YuLauncher.Core.Window.Pages"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="ja-JP"
        lex:ResxLocalizationProvider.DefaultAssembly="YuLauncher"
        lex:ResxLocalizationProvider.DefaultDictionary="Language"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="{lex:Loc CreateGameDialogTitle}"
        Width="600" Height="380"
        MinWidth="500" MinHeight="320"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{lex:Loc CreateGameDialogTitle}"
                     Height="48"/>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="0,0,0,0">
            <Grid Margin="16,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 名前 -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBlock Grid.Column="0"
                                  Text="{lex:Loc SimpleName}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,12,0"
                                  Appearance="Primary"/>
                    <ui:TextBox Grid.Column="1"
                                x:Name="NameBox"
                                PlaceholderText="{lex:Loc NameInput}"/>
                </Grid>

                <!-- ファイル選択 / URL -->
                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ui:Button Grid.Column="0"
                                   x:Name="UrlButton"
                                   Content="{lex:Loc SelectFileLabel}"
                                   Click="UrlButton_OnClick"
                                   Margin="0,0,8,0"/>
                        <ui:TextBlock Grid.Column="1"
                                      x:Name="PathLabel"
                                      VerticalAlignment="Center"
                                      TextWrapping="NoWrap"
                                      TextTrimming="CharacterEllipsis"
                                      Appearance="Secondary"/>
                    </Grid>
                    <ui:TextBox Grid.Row="1"
                                x:Name="UrlBlock"
                                PlaceholderText="{lex:Loc PleaseURL}"
                                Visibility="Collapsed"/>
                </Grid>

                <!-- ジャンル選択 -->
                <Grid Grid.Row="2" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:TextBlock Grid.Column="0"
                                  Text="{lex:Loc SimpleGenreManage}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,12,0"
                                  Appearance="Primary"/>
                    <ComboBox Grid.Column="1"
                              x:Name="GenreSelectComboBox"
                              Loaded="GenreSelectComboBox_OnLoaded"
                              SelectionChanged="GenreSelectComboBox_OnSelectionChanged">
                        <ComboBoxItem x:Name="GenreApplicationComboBoxItem" Content="{lex:Loc GenreApplication}"/>
                        <ComboBoxItem x:Name="GenreWebSiteComboBoxItem" Content="{lex:Loc GenreWebsite}"/>
                        <ComboBoxItem x:Name="GenreWebGameComboBoxItem" Content="{lex:Loc GenreWebGame}"/>
                        <ComboBoxItem x:Name="GenreWebSaverComboBoxItem" Content="{lex:Loc GenreWebSaver}"/>
                    </ComboBox>
                </Grid>

                <!-- ジャンル案内 -->
                <TextBlock Grid.Row="3"
                           x:Name="GenreLabel"
                           Text="{lex:Loc PleaseSelectGenre}"
                           Visibility="Collapsed"
                           Margin="0,0,0,8"
                           FontWeight="Bold"
                           Foreground="Yellow"/>

                <!-- エラー表示 -->
                <TextBlock Grid.Row="4"
                           x:Name="ErrLabel"
                           Text="{lex:Loc ErrPickPath}"
                           Visibility="Collapsed"
                           Margin="0,0,0,8"
                           Foreground="Red"/>

                <!-- 作成ボタン -->
                <StackPanel Grid.Row="6"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <ui:Button x:Name="CreateButton"
                               Content="{lex:Loc CreateButton}"
                               Click="CreateButton_OnClick"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
